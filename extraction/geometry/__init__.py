"""Spatial reasoning over a drawing: what a dimension line measures, and where things sit.

Geometry is where a correctly-read number gets attached to the wrong item. The dual-unit lane
corroborates that `984` was *read* correctly and says nothing about *what it measures* — so an error
here produces a finding that is internally consistent, fully traced and completely wrong.

Source: `docs/DESIGN_EXTRACTION.md` §6
"""

from __future__ import annotations
