"""Synthetic gold-case definition for the first honest countertop PASS.

The bytes are generated by the test from these authored facts. Keeping the case definition here,
rather than under ``eval/gold_set/cases/``, avoids committing anything that looks like a reviewed
client package while still pinning the answer-key shape this regression exercises.

Source: issue #621 · Verification: tests/eval/test_first_pass_case.py
"""

from __future__ import annotations

from datetime import date
from uuid import UUID

from vocabulary.semantic_types import SemanticType

CASE_ID = "synthetic-countertop-depth-pass-01"
PROJECT_ID = UUID("62100000-0000-4000-8000-000000000000")
DOCUMENT_VERSION_ID = UUID("62100000-0000-4000-8000-000000000001")
PASS_READING = "648 [25 1/2]"
PASS_EXACT_INCHES = "51/2"
FAIL_READING = '25.53937"'
FAIL_EXACT_INCHES = "253937/10000"


def answer_key(
    *,
    raw_text: str,
    exact_inches: str,
    shop_hash: str,
    expected_outcome: str,
) -> dict[str, object]:
    """Return a GOLD_SET_FORMAT-compatible answer key for one generated shop drawing."""
    return {
        "id": CASE_ID,
        "product_type": "countertop",
        "arch": "arch.pdf",
        "shop": "shop.pdf",
        "ground_truth": {
            "observations": [
                {
                    "semantic_type": SemanticType.CT010.value,
                    "source": "SHOP",
                    "value": {"exact": exact_inches, "unit": "in", "raw_text": raw_text},
                    "page": 1,
                    "polygon": [40, 55, 130, 85],
                    "item_id": "S_CT_DEPTH_1",
                }
            ],
            "matches": [],
            "expected_findings": [
                {
                    "check": "CT-DEPTH-001",
                    "outcome": expected_outcome,
                    "reason": "countertop depth equals cabinet depth plus overhang",
                }
            ],
        },
        "provenance": {
            "annotator": "synthetic-first-pass-case",
            "annotated_on": date(2026, 9, 16).isoformat(),
            "documents": [
                {
                    "source": "SHOP",
                    "document_version_id": str(DOCUMENT_VERSION_ID),
                    "content_hash": shop_hash,
                }
            ],
        },
    }
