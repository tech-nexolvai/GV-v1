"""Writing evidence into the database.

`evidence/` decides what a reading means and this stores it, on the `app/` side of the line
`docs/DESIGN_PLATFORM.md` draws — the same split `app/verdicts/` makes for findings.
"""
